# Data Quality Guided Multi Task Allocation Based on Bi-level Deep Reinforcement Learning in Crowdsensing
## Requirements
```
requests==2.28.1
pandas==2.3.0
numpy==1.26.0
matplotlib==3.6.2
PyYAML==6.0
torch==2.1.0
The experiment for DQBDRL is run on the Windows 11 system equipped with an Intel(R)Core(TM) i5-4590 CPU and Python 3.9.
```
## Usage
```
run DQBDRL
```

```python
python mulTask.py ```
